% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dist2.R
\name{dist2}
\alias{dist2}
\title{Compute Euclidean Distance Between a Point and Multiple Points}
\usage{
dist2(pt1, pt2)
}
\arguments{
\item{pt1}{A data frame or matrix with one row and two columns (x, y), representing the reference point.}

\item{pt2}{A data frame or matrix with multiple rows and two columns (x, y), representing target points.}
}
\value{
A numeric vector of distances from \code{pt1} to each row in \code{pt2}.
}
\description{
This function calculates the Euclidean distance between a single reference point (\code{pt1})
and multiple target points (\code{pt2}). It is fully vectorized for efficiency.
}
\details{
The function computes distances using the standard Euclidean formula:
\deqn{d = \sqrt{(x_2 - x_1)^2 + (y_2 - y_1)^2}}
}
\examples{
pt1 <- data.frame(x = 1, y = 2)
pt2 <- data.frame(x = c(3, 4), y = c(5, 6))
dist2(pt1, pt2)

}
