% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sp_norm.R
\name{sp_norm}
\alias{sp_norm}
\title{Normalize Segmentation Indicators}
\usage{
sp_norm(x, x.min, x.max)
}
\arguments{
\item{x}{Numeric vector to be normalized.}

\item{x.min}{Minimum value used for normalization (typically the observed or theoretical minimum of \code{x}).}

\item{x.max}{Maximum value used for normalization (typically the observed or theoretical maximum of \code{x}).}
}
\value{
A numeric vector with values scaled between 0 and 1.
}
\description{
Applies min-max normalization to a numeric vector, scaling values to the range 0-1.
}
\details{
This function is used to normalize the segmentation metrics. The formula used is:
\deqn{(x - x.min) / (x.max - x.min)}

If \code{x.min == x.max}, the result will contain \code{NaN} or \code{Inf} due to division by zero — users should check input ranges beforehand.
}
\examples{
values <- c(0.2, 0.5, 0.9)
sp_norm(values, x.min = 0.2, x.max = 0.9)

}
